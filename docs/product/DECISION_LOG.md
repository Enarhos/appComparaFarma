# Decision Log

Registro cronológico de decisiones y cierres relevantes. Cada entrada: fecha, qué se decidió/cerró, referencia.

| Fecha | Entrada |
|---|---|
| 2026-07-19 | **CF-108 (Migrar mobile a `@comparafarma/domain`) cerrado retroactivamente.** El código ya estaba implementado y en `main` (paquete `packages/domain` consumido por `mobile/`, `CACHE_PREFIX` en `search_cache_v10_`, `matchKey` migrado), pero el issue seguía marcado `Estado: Pendiente`. Verificado por `/docs-steward` — ver `docs/engineering/issues/CF-108_MIGRATE_MOBILE.md`. |
| 2026-07-19 | **Reforzado el monitoreo de producción**: `monitor-api.yml` pasa de cada 6h a cada 1h y cubre las 9 farmacias (antes 4); rate limiting migrado de memoria a Upstash Redis (resuelve RL-01 de `ER-002`); Sentry agregado al backend (`api/src/lib/sentry.ts`, condicional a `SENTRY_DSN`); issues de fallo del monitor se auto-asignan al owner para notificación por email. |
| 2026-07-19 | **PM-001 — pipeline de deploy del backend estuvo roto en producción** (`/api/search` devolvía 500). Causas raíz: la CI subía solo `api/` (sin `packages/domain`) al deployar, y `packages/domain` nunca se compilaba a JS real. Resuelto: deploy desde la raíz del monorepo + Root Directory `api` en Vercel + glob explícito de functions + build real de `packages/domain` vía `postinstall`. Detalle completo en `docs/engineering/postmortems/PM-001_DEPLOY_PIPELINE_BROKEN.md`. |
| 2026-07-20 | **Fase 1 de `COMPANY_STRATEGY.md` completada: historial de precios en Supabase.** Nueva tabla `price_history` (esquema genérico — `channels` jsonb, sin columnas hardcodeadas por farmacia como `cmr_price`/`sbpay_price`) recibiendo datos reales de producción vía `api/src/lib/priceHistoryDb.ts`. Verificado en vivo: 122 filas reales tras un puñado de búsquedas, con nombres de programas de fidelización correctos (CMR, Online, T. Más, SBPay, Plus, Fonasa) según cada farmacia. No tocó `mobile/` ni `packages/domain`. |
| 2026-07-20 | **Fase 1 cerrada completa: tracking de clicks a farmacia.** `onlineUrl` ahora pasa por `/api/go` (nuevo), que valida el dominio de destino contra el dominio real de cada farmacia (previene open redirect), registra el click en `pharmacy_clicks`, y redirige. Base de datos para negociar afiliación a futuro. Verificado en producción: redirect 302 real, bloqueo 400 de dominio ajeno probado, click registrado correctamente. `mobile/` no cambia — sigue abriendo lo que venga en `onlineUrl` sin saber que ahora pasa por un redirect propio. |
| 2026-07-20 | **Fase 2a — nuevo workspace `web/` (Next.js), código completo y verificado localmente.** Home + `/buscar/[query]` con metadata dinámica por medicamento, consumiendo el mismo `api/` sin tocarlo. Heredó gratis el tracking de clicks de `/api/go` (los links de farmacia ya vienen reescritos desde la API, sin código extra en `web/`). No toca `mobile/`. Falta: crear el proyecto en Vercel y deployar. |
| 2026-07-20 | **Fase 2a deployada a producción**: `https://app-compara-farma-web.vercel.app` (proyecto Vercel propio, auto-deploy on-push, sin job de CI como `api/`). Rediseño visual completo con el skill `artifact-design` (tipografía Fraunces/Figtree, paleta propia, hero con demo de ahorro) y paridad de información con mobile (imagen del producto, desglose de canales de precio, indicador de stock). Verificado contra datos reales de producción. |
| 2026-07-20/21 | **Fase 3 — panel `/admin` en `web/`, primera versión: dashboard de clicks.** Protegido con Supabase Auth (login con Google OAuth + email/contraseña) y lista blanca `ADMIN_ALLOWED_EMAILS` — necesaria porque Supabase auto-provisiona cualquier cuenta de Google que llegue sin ella. Visualiza los datos de `pharmacy_clicks` de Fase 1 (total y últimos 7 días por farmacia). Verificado en producción con la cuenta real del usuario. |
| 2026-07-20/21 | **Fase 3 — config de farmacias/banner y bandeja de feedback.** Nuevas tablas genéricas `app_config` (clave/valor) y `feedback` en Supabase, documentadas por primera vez en `docs/database/schema.sql` (antes las tablas de Fase 1 se habían creado a mano sin dejar rastro en el repo). `api/` lee la config con fallback automático a las env vars existentes (`DISABLED_PHARMACIES`, `DONATION_BANNER_*`) si la DB falla — el contrato de `/api/config` no cambió, cero riesgo para `mobile/`. Confirmado en producción que togglear el banner desde `/admin/config` cambia lo que devuelve `/api/config`; el retraso en verse reflejado *dentro* de la app es un problema separado de `mobile/` (no refetchea en runtime), anotado en `BACKLOG_PRODUCT.md` como `v15-16`. |
| 2026-07-22 | **SEO reforzado**: `sitemap.xml` (Home + búsquedas frecuentes curadas de `QUICK_SEARCHES`), `robots.txt` (bloquea `/admin`), y JSON-LD `Product`/`AggregateOffer` en `/buscar/[query]` (top 20 resultados más baratos). Encontrado y corregido en el camino: faltaba por completo la variable `SITE_URL` en Vercel, lo que hacía que el sitemap apuntara a una URL placeholder en vez de la real — corregida y verificada en producción. |
| 2026-07-22 | **Estabilización de calidad de `web/` — mergeado a `main`.** PR #9, rama `web/stabilization-quality-pass`, commit `7714849`. Alcance: (1) lint reparado (Next.js 16 removió `next lint`; `eslint.config.mjs` importa las configs flat nativas de `eslint-config-next` en vez de pasarlas por `FlatCompat`, que rompía con "circular structure to JSON"); (2) `loading.tsx`/`error.tsx`/`not-found.tsx` globales y específicos de `/buscar/[query]`; (3) `SearchBox` agregado a la página de resultados; (4) primera suite de tests del proyecto en `web/` (Vitest + Testing Library, 13 tests/5 archivos). Typecheck/lint/test/build de `web/`, tests de `packages/domain` y de `api/`, y typecheck del monorepo completo — todos verdes; cero cambios en `mobile/`. Se descubrió y documentó que este monorepo (`node-linker=hoisted`) duplica físicamente `react` en disco, rompiendo tests de componentes hasta alinear el alias en `web/vitest.config.ts` — detalle en `reference-comparafarma-docs` (memoria). |
| 2026-07-23 | **Sprint 2 — ficha permanente de medicamento (`/medicamento/[slug]`) — mergeado a `main`.** PR #20, rama `web/medication-detail-page`, commit `39d21ad`. Identidad sigue siendo `matchKey` (sin CFM-ID todavía); el slug es "{nombre-legible}-{hash FNV-1a 64-bit de matchKey}", resuelto 100% en `web/` reusando `/api/search` tal cual (sin endpoint ni tabla nueva). Resolución nunca desempata por precio: 0 coincidencias → 404, 1 → ok, 2+ → falla segura con log, nunca elige ganador. Redirect permanente al slug vigente + `alternates.canonical` siempre apuntando a ese vigente. `robots: {index:false, follow:true}` — sin sumar a `sitemap.ts` este sprint. 39 tests nuevos, todos verdes. Verificado en dev real: ficha con datos de producción, redirect de slug desactualizado confirmado navegando, enlaces trackeados vía `/api/go`. |
| 2026-07-23 | **Bug de precio en EasyFarma diagnosticado y corregido — mergeado a `main`.** PR #21, rama `fix/easyfarma-discontinued-plus-price`, commit `07179d9`. Reporte real: "Insulina Apidra Solostar 1 Unidad" mostraba $5.240 en ComparaFarma vs. $9.990 en la página real de EasyFarma. Causa raíz: el scraper leía un bloque `product-price d-none` ("Easyfarma Plus") — oculto por CSS en toda tarjeta, con el link de navegación al programa comentado en el HTML del sitio (programa descontinuado, no honrado en ninguna página de producto) — y lo mapeaba como `cmrPrice`. Como `effectivePrice()` toma el mínimo entre canales (genérico, sin cambios), ese precio fantasma ganaba siempre. Muestreo de 155 productos en 10 categorías: 147 (94.8%) afectados — sistémico, no un caso aislado. Corrección acotada a `api/src/clients/easyfarma.ts` (`cmrPrice` siempre `null` para EasyFarma); `effectivePrice`/`toPharmacyPrice`/`mergeDuplicates`/`matchKey` sin tocar. Tests nuevos con fixture HTML local. Verificado con HTML real para insulina apidra/paracetamol/ibuprofeno/omeprazol/losartán — 0% con `cmrPrice` residual en los 5. |
| 2026-07-27 | **Sprint Web 1 — histórico de precios en la ficha de medicamento, mergeado a `main`.** PR #24, rama `web/medication-price-history`, commit `e9bacb4`. Nuevo endpoint aditivo `GET /api/price-history?matchKey=&days=` (capa de lectura separada en `api/src/lib/priceHistoryQuery.ts`, sin mezclarse con `recordPriceHistory`), leyendo la misma tabla `price_history` de Fase 1 — sin tabla nueva. Responde 200 con historial vacío si Supabase no está configurado o falla (nunca rompe la ficha). La ficha `/medicamento/[slug]` (PR #20) se extendió con mínimo/máximo histórico, variación 7d/30d y un gráfico SVG por farmacia — **decisión clave**: el RFC-002 original pedía una ruta nueva `/medicamento/[matchKey]`, pero esa ruta colisiona con `[slug]` ya en producción (Next.js no permite dos nombres de segmento dinámico distintos en el mismo path); se adaptó extendiendo la ficha existente en vez de crear una ruta competidora. 41 tests API + 56 tests web nuevos. `mobile/` y `packages/domain/` sin cambios. |
| 2026-07-27 | **Sprint Web 2 — UX, gráfico central e insights automáticos, mergeado a `main`.** PR #25, rama `web/ui-improvements` (basada en la del Sprint Web 1, no en `main` directamente), commit `329c563`. Gráfico de histórico casi el doble de alto (pasa a ser el centro visual de la ficha) con línea de referencia punteada en el mínimo histórico; paleta de colores por farmacia rediseñada (la anterior tenía colisiones reales — Ahumada/Dr. Simi eran dos rojos casi idénticos, Cruz Verde/EasyFarma dos verdes casi idénticos); tarjetas de farmacia rediseñadas (`PharmacyPriceCard`, badge "Mejor precio" + botón real "Ir a la farmacia →"); cabecera de la ficha compactada; nueva sección "Insights" con frases generadas dinámicamente (variación semanal, farmacia más barata/cara, dispersión de precios) que se ocultan si no hay datos suficientes; Twitter Card + `Offer` por farmacia en el Schema.org de la ficha. Verificado en vivo contra datos reales de producción (7 farmacias con histórico real para paracetamol). Se encontraron y corrigieron dos bugs reales durante esa verificación: título duplicado "| ComparaFarma | ComparaFarma" (mismo bug ya resuelto una vez en el Sprint 2 original, reintroducido por el título nuevo del Sprint Web 1) y el badge "Bioequivalente" cortándose fuera del viewport en mobile (375px, faltaba `flex-wrap`). 78 tests web (22 nuevos). Sin cambios en `mobile/`, `packages/domain/` ni el contrato de `/api/search`. |
| 2026-07-28 | **Se formalizan 3 subagentes nuevos** (`comparafarma-software-factory`, `comparafarma-qa`, `comparafarma-devops` en `.claude/agents/`), complementando al `comparafarma-cto` ya existente, sin modificar la metodología congelada del Acta 2026-07-27. Mario asume explícitamente el rol de **CEO** en el flujo de gobernanza. El CTO evaluó el estado real del proyecto (verificado contra código, no solo docs): B-3 (Algolia hardcodeada) y B-2 (`API_SECRET_KEY`) ya estaban resueltos; `CF-111` (timeout AraucoMed, sin resolver 9 días) se formalizó en `BACKLOG_PRODUCT.md` con CFPS 3.2; **B-1 (Data Safety en Play Console)** sigue siendo el único bloqueador real para pasar a Producción. Sesión pausada a pedido del CEO — queda pendiente elegir entre B-1, CF-111, ambos en paralelo, u otro foco. Detalle completo en `docs/actas/20260728.md` (sección "Actualización de la sesión"). |
| 2026-07-31 | **B-1 y CF-111 se ejecutan en paralelo** (decisión CTO, retomando el Acta 2026-07-28 — ver `docs/actas/20260731.md`): no compiten por el mismo recurso, B-1 es acción exclusiva de Mario en Play Console (`PRODUCTION_BLOCKERS_PLAN.md`), CF-111 es investigación técnica. **CF-111 cerrado el mismo día**: 5 corridas contra `/api/search?debug=1` en producción, las 5 con `araucomed` en `status: "fulfilled"` (300–1897ms, muy bajo el timeout de 8000ms) — el fallo no es persistente, se descarta bloqueo sistemático por IP de Vercel. Queda en monitoreo pasivo vía `Monitor API` (cada hora, ya cubre las 9 farmacias); se reabre si reaparece. Detalle en `docs/engineering/issues/CF-111_INVESTIGATE_ARAUCOMED_TIMEOUT.md`. No se tocó código (`api/src/clients/araucomed.ts` sin cambios) ni `mobile/`. |
