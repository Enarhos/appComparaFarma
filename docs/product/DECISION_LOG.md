# Decision Log

Registro cronológico de decisiones y cierres relevantes. Cada entrada: fecha, qué se decidió/cerró, referencia.

| Fecha | Entrada |
|---|---|
| 2026-07-19 | **CF-108 (Migrar mobile a `@comparafarma/domain`) cerrado retroactivamente.** El código ya estaba implementado y en `main` (paquete `packages/domain` consumido por `mobile/`, `CACHE_PREFIX` en `search_cache_v10_`, `matchKey` migrado), pero el issue seguía marcado `Estado: Pendiente`. Verificado por `/docs-steward` — ver `docs/engineering/issues/CF-108_MIGRATE_MOBILE.md`. |
| 2026-07-19 | **Reforzado el monitoreo de producción**: `monitor-api.yml` pasa de cada 6h a cada 1h y cubre las 9 farmacias (antes 4); rate limiting migrado de memoria a Upstash Redis (resuelve RL-01 de `ER-002`); Sentry agregado al backend (`api/src/lib/sentry.ts`, condicional a `SENTRY_DSN`); issues de fallo del monitor se auto-asignan al owner para notificación por email. |
| 2026-07-19 | **PM-001 — pipeline de deploy del backend estuvo roto en producción** (`/api/search` devolvía 500). Causas raíz: la CI subía solo `api/` (sin `packages/domain`) al deployar, y `packages/domain` nunca se compilaba a JS real. Resuelto: deploy desde la raíz del monorepo + Root Directory `api` en Vercel + glob explícito de functions + build real de `packages/domain` vía `postinstall`. Detalle completo en `docs/engineering/postmortems/PM-001_DEPLOY_PIPELINE_BROKEN.md`. |
| 2026-07-20 | **Fase 1 de `COMPANY_STRATEGY.md` completada: historial de precios en Supabase.** Nueva tabla `price_history` (esquema genérico — `channels` jsonb, sin columnas hardcodeadas por farmacia como `cmr_price`/`sbpay_price`) recibiendo datos reales de producción vía `api/src/lib/priceHistoryDb.ts`. Verificado en vivo: 122 filas reales tras un puñado de búsquedas, con nombres de programas de fidelización correctos (CMR, Online, T. Más, SBPay, Plus, Fonasa) según cada farmacia. No tocó `mobile/` ni `packages/domain`. |
| 2026-07-20 | **Fase 1 cerrada completa: tracking de clicks a farmacia.** `onlineUrl` ahora pasa por `/api/go` (nuevo), que valida el dominio de destino contra el dominio real de cada farmacia (previene open redirect), registra el click en `pharmacy_clicks`, y redirige. Base de datos para negociar afiliación a futuro. Verificado en producción: redirect 302 real, bloqueo 400 de dominio ajeno probado, click registrado correctamente. `mobile/` no cambia — sigue abriendo lo que venga en `onlineUrl` sin saber que ahora pasa por un redirect propio. |
| 2026-07-20 | **Fase 2a — nuevo workspace `web/` (Next.js), código completo y verificado localmente.** Home + `/buscar/[query]` con metadata dinámica por medicamento, consumiendo el mismo `api/` sin tocarlo. Heredó gratis el tracking de clicks de `/api/go` (los links de farmacia ya vienen reescritos desde la API, sin código extra en `web/`). No toca `mobile/`. Falta: crear el proyecto en Vercel y deployar. |
| 2026-07-20 | **Fase 2a deployada a producción**: `https://app-compara-farma-web.vercel.app` (proyecto Vercel propio, auto-deploy on-push, sin job de CI como `api/`). Rediseño visual completo con el skill `artifact-design` (tipografía Fraunces/Figtree, paleta propia, hero con demo de ahorro) y paridad de información con mobile (imagen del producto, desglose de canales de precio, indicador de stock). Verificado contra datos reales de producción. |
| 2026-07-20/21 | **Fase 3 — panel `/admin` en `web/`, primera versión: dashboard de clicks.** Protegido con Supabase Auth (login con Google OAuth + email/contraseña) y lista blanca `ADMIN_ALLOWED_EMAILS` — necesaria porque Supabase auto-provisiona cualquier cuenta de Google que llegue sin ella. Visualiza los datos de `pharmacy_clicks` de Fase 1 (total y últimos 7 días por farmacia). Verificado en producción con la cuenta real del usuario. |
| 2026-07-20/21 | **Fase 3 — config de farmacias/banner y bandeja de feedback.** Nuevas tablas genéricas `app_config` (clave/valor) y `feedback` en Supabase, documentadas por primera vez en `docs/database/schema.sql` (antes las tablas de Fase 1 se habían creado a mano sin dejar rastro en el repo). `api/` lee la config con fallback automático a las env vars existentes (`DISABLED_PHARMACIES`, `DONATION_BANNER_*`) si la DB falla — el contrato de `/api/config` no cambió, cero riesgo para `mobile/`. Confirmado en producción que togglear el banner desde `/admin/config` cambia lo que devuelve `/api/config`; el retraso en verse reflejado *dentro* de la app es un problema separado de `mobile/` (no refetchea en runtime), anotado en `BACKLOG_PRODUCT.md` como `v15-16`. |
| 2026-07-22 | **SEO reforzado**: `sitemap.xml` (Home + búsquedas frecuentes curadas de `QUICK_SEARCHES`), `robots.txt` (bloquea `/admin`), y JSON-LD `Product`/`AggregateOffer` en `/buscar/[query]` (top 20 resultados más baratos). Encontrado y corregido en el camino: faltaba por completo la variable `SITE_URL` en Vercel, lo que hacía que el sitemap apuntara a una URL placeholder en vez de la real — corregida y verificada en producción. |
| 2026-07-22 | **Estabilización de calidad de `web/` — mergeado a `main`.** PR #9, rama `web/stabilization-quality-pass`, commit `7714849`. Alcance: (1) lint reparado (Next.js 16 removió `next lint`; `eslint.config.mjs` importa las configs flat nativas de `eslint-config-next` en vez de pasarlas por `FlatCompat`, que rompía con "circular structure to JSON"); (2) `loading.tsx`/`error.tsx`/`not-found.tsx` globales y específicos de `/buscar/[query]`; (3) `SearchBox` agregado a la página de resultados; (4) primera suite de tests del proyecto en `web/` (Vitest + Testing Library, 13 tests/5 archivos). Typecheck/lint/test/build de `web/`, tests de `packages/domain` y de `api/`, y typecheck del monorepo completo — todos verdes; cero cambios en `mobile/`. Se descubrió y documentó que este monorepo (`node-linker=hoisted`) duplica físicamente `react` en disco, rompiendo tests de componentes hasta alinear el alias en `web/vitest.config.ts` — detalle en `reference-comparafarma-docs` (memoria). |
| 2026-07-23 | **Sprint 2 — ficha permanente de medicamento (`/medicamento/[slug]`) — mergeado a `main`.** PR #20, rama `web/medication-detail-page`, commit `39d21ad`. Identidad sigue siendo `matchKey` (sin CFM-ID todavía); el slug es "{nombre-legible}-{hash FNV-1a 64-bit de matchKey}", resuelto 100% en `web/` reusando `/api/search` tal cual (sin endpoint ni tabla nueva). Resolución nunca desempata por precio: 0 coincidencias → 404, 1 → ok, 2+ → falla segura con log, nunca elige ganador. Redirect permanente al slug vigente + `alternates.canonical` siempre apuntando a ese vigente. `robots: {index:false, follow:true}` — sin sumar a `sitemap.ts` este sprint. 39 tests nuevos, todos verdes. Verificado en dev real: ficha con datos de producción, redirect de slug desactualizado confirmado navegando, enlaces trackeados vía `/api/go`. |
| 2026-07-23 | **Bug de precio en EasyFarma diagnosticado y corregido — mergeado a `main`.** PR #21, rama `fix/easyfarma-discontinued-plus-price`, commit `07179d9`. Reporte real: "Insulina Apidra Solostar 1 Unidad" mostraba $5.240 en ComparaFarma vs. $9.990 en la página real de EasyFarma. Causa raíz: el scraper leía un bloque `product-price d-none` ("Easyfarma Plus") — oculto por CSS en toda tarjeta, con el link de navegación al programa comentado en el HTML del sitio (programa descontinuado, no honrado en ninguna página de producto) — y lo mapeaba como `cmrPrice`. Como `effectivePrice()` toma el mínimo entre canales (genérico, sin cambios), ese precio fantasma ganaba siempre. Muestreo de 155 productos en 10 categorías: 147 (94.8%) afectados — sistémico, no un caso aislado. Corrección acotada a `api/src/clients/easyfarma.ts` (`cmrPrice` siempre `null` para EasyFarma); `effectivePrice`/`toPharmacyPrice`/`mergeDuplicates`/`matchKey` sin tocar. Tests nuevos con fixture HTML local. Verificado con HTML real para insulina apidra/paracetamol/ibuprofeno/omeprazol/losartán — 0% con `cmrPrice` residual en los 5. |
| 2026-07-27 | **Sprint Web 1 — histórico de precios en la ficha de medicamento, mergeado a `main`.** PR #24, rama `web/medication-price-history`, commit `e9bacb4`. Nuevo endpoint aditivo `GET /api/price-history?matchKey=&days=` (capa de lectura separada en `api/src/lib/priceHistoryQuery.ts`, sin mezclarse con `recordPriceHistory`), leyendo la misma tabla `price_history` de Fase 1 — sin tabla nueva. Responde 200 con historial vacío si Supabase no está configurado o falla (nunca rompe la ficha). La ficha `/medicamento/[slug]` (PR #20) se extendió con mínimo/máximo histórico, variación 7d/30d y un gráfico SVG por farmacia — **decisión clave**: el RFC-002 original pedía una ruta nueva `/medicamento/[matchKey]`, pero esa ruta colisiona con `[slug]` ya en producción (Next.js no permite dos nombres de segmento dinámico distintos en el mismo path); se adaptó extendiendo la ficha existente en vez de crear una ruta competidora. 41 tests API + 56 tests web nuevos. `mobile/` y `packages/domain/` sin cambios. |
| 2026-07-27 | **Sprint Web 2 — UX, gráfico central e insights automáticos, mergeado a `main`.** PR #25, rama `web/ui-improvements` (basada en la del Sprint Web 1, no en `main` directamente), commit `329c563`. Gráfico de histórico casi el doble de alto (pasa a ser el centro visual de la ficha) con línea de referencia punteada en el mínimo histórico; paleta de colores por farmacia rediseñada (la anterior tenía colisiones reales — Ahumada/Dr. Simi eran dos rojos casi idénticos, Cruz Verde/EasyFarma dos verdes casi idénticos); tarjetas de farmacia rediseñadas (`PharmacyPriceCard`, badge "Mejor precio" + botón real "Ir a la farmacia →"); cabecera de la ficha compactada; nueva sección "Insights" con frases generadas dinámicamente (variación semanal, farmacia más barata/cara, dispersión de precios) que se ocultan si no hay datos suficientes; Twitter Card + `Offer` por farmacia en el Schema.org de la ficha. Verificado en vivo contra datos reales de producción (7 farmacias con histórico real para paracetamol). Se encontraron y corrigieron dos bugs reales durante esa verificación: título duplicado "| ComparaFarma | ComparaFarma" (mismo bug ya resuelto una vez en el Sprint 2 original, reintroducido por el título nuevo del Sprint Web 1) y el badge "Bioequivalente" cortándose fuera del viewport en mobile (375px, faltaba `flex-wrap`). 78 tests web (22 nuevos). Sin cambios en `mobile/`, `packages/domain/` ni el contrato de `/api/search`. |
| 2026-07-28 | **Se formalizan 3 subagentes nuevos** (`comparafarma-software-factory`, `comparafarma-qa`, `comparafarma-devops` en `.claude/agents/`), complementando al `comparafarma-cto` ya existente, sin modificar la metodología congelada del Acta 2026-07-27. Mario asume explícitamente el rol de **CEO** en el flujo de gobernanza. El CTO evaluó el estado real del proyecto (verificado contra código, no solo docs): B-3 (Algolia hardcodeada) y B-2 (`API_SECRET_KEY`) ya estaban resueltos; `CF-111` (timeout AraucoMed, sin resolver 9 días) se formalizó en `BACKLOG_PRODUCT.md` con CFPS 3.2; **B-1 (Data Safety en Play Console)** sigue siendo el único bloqueador real para pasar a Producción. Sesión pausada a pedido del CEO — queda pendiente elegir entre B-1, CF-111, ambos en paralelo, u otro foco. Detalle completo en `docs/actas/20260728.md` (sección "Actualización de la sesión"). |
| 2026-07-31 | **B-1 y CF-111 se ejecutan en paralelo** (decisión CTO, retomando el Acta 2026-07-28 — ver `docs/actas/20260731.md`): no compiten por el mismo recurso, B-1 es acción exclusiva de Mario en Play Console (`PRODUCTION_BLOCKERS_PLAN.md`), CF-111 es investigación técnica. **CF-111 cerrado el mismo día**: 5 corridas contra `/api/search?debug=1` en producción, las 5 con `araucomed` en `status: "fulfilled"` (300–1897ms, muy bajo el timeout de 8000ms) — el fallo no es persistente, se descarta bloqueo sistemático por IP de Vercel. Queda en monitoreo pasivo vía `Monitor API` (cada hora, ya cubre las 9 farmacias); se reabre si reaparece. Detalle en `docs/engineering/issues/CF-111_INVESTIGATE_ARAUCOMED_TIMEOUT.md`. No se tocó código (`api/src/clients/araucomed.ts` sin cambios) ni `mobile/`. |
| 2026-07-31 | **Ratificada la secuencia de Sprints 0–F con scoring CFPS** (ver `docs/actas/20260731b.md` y `BACKLOG_PRODUCT.md`): orden E → A → C → spike de bioequivalencia → B, con D/F en backlog futuro. Hallazgo clave durante el scoring: `isBioequivalent` no tiene fuente de verdad regulatoria (heterogéneo entre las 9 farmacias, `false` hardcodeado en 2 de ellas) — Sprint B queda bloqueado hasta resolver esa fuente de datos, por el Principio 7 del Libro Fundacional. |
| 2026-07-31 | **Sprint E — comparación de receta completa, mergeado a `main`.** PR #29, rama `web/stabilization-quality-pass`, commit `8d007bf`. "Mi receta" en `web/`: lista de hasta 8 medicamentos con comparación entre comprar todo en una farmacia vs. repartir la compra en la mejor farmacia por ítem — sin cuenta de usuario, sin link para compartir (persistencia en `localStorage` vía `useSyncExternalStore`). Reutiliza `/api/search` tal cual. 105/105 tests de `web/` verdes. Sin cambios en `mobile/`, `packages/domain/` ni el contrato de `/api/search`. |
| 2026-07-31 | **Bug de stock en AraucoMed diagnosticado y corregido — mergeado a `main`.** PR #30, commit `f8f2edb`. Reporte real: "Medicasp 1% Shampoo 130ml" agotado en el sitio de AraucoMed pero mostrado como disponible en ComparaFarma. Causa raíz: el campo `active` del JSON de búsqueda de AraucoMed (PrestaShop) indica solo "publicado en catálogo", no stock real — AraucoMed sigue publicando productos agotados con `active:1`, y el JSON no expone ningún campo directo de inventario. Fix: `api/src/clients/araucomed.ts` ahora lee el HTML embebido en `rendered_products` (mismo JSON) para detectar la clase `out-of-stock`/`pst-bar-info-oos` por producto, con fallback al comportamiento anterior si ese HTML no viene. 2 tests nuevos + 48/48 tests de `api/` verdes. Acotado a `api/`, sin tocar `mobile/` ni `packages/domain/`. |
| 2026-07-31 | **Sprint A — RFC-002 (Canonical Medication Registry / CFM-ID), Fases 0–5 implementadas y mergeadas a `main`.** Identidad permanente (`CFM-######`) por medicamento, coexistiendo con `matchKey` (que no se toca) vía tabla de alias `match_key → cfm_id` — absorbe futuros cambios de versión del algoritmo de matching sin perder continuidad de `price_history`/alertas/favoritos. Cambios: `cfmId?: string \| null` aditivo en `packages/domain/src/types.ts`; nuevo `api/src/lib/medicationRegistry.ts` (degradación elegante si Supabase no responde, mismo patrón que `priceHistoryDb.ts`); wiring en `searchService.ts` antes de `recordPriceHistory`; backfill de `cfm_id` en `priceHistoryDb.ts` y `clickTracking.ts`/`go.ts` (vía query param en la URL trackeada). SQL de las tablas nuevas (`medications`, `medication_match_key_aliases`) agregado a `docs/database/schema.sql`. **Corrido en Supabase por Mario el mismo día** — el registro ya está activo en producción. 6 tests nuevos de `medicationRegistry.ts` + 1 de integración en `searchService.test.ts`; 55/55 tests de `api/`, 38/38 de `packages/domain` verdes; typecheck limpio en los 4 workspaces. Cero cambios en `mobile/`, cero cambios en `matchKey`/`mergeDuplicates`/`effectivePrice`. |
| 2026-07-31 | **Spike de datos de bioequivalencia cerrado — desbloquea B parcialmente.** Fuente encontrada: `datos.gob.cl` (dataset 1303 del ISP, API real vía CKAN DataStore) con `Principio Activo`, `Producto`, `Registro` (N° ISP), `Titular`, `Vigencia` — confirmado con datos reales, no vacío. El obstáculo pasa de "no hay dato" a "hay que cruzarlo contra el catálogo sin caer en fuzzy matching riesgoso". Investigación de las 9 farmacias: **Dr. Simi y Farmex ya exponen el Registro ISP en el JSON que consultamos hoy** (descartado al mapear) — match exacto viable de inmediato, cero riesgo de falso positivo. AraucoMed, Sermecoop y EasyFarma lo exponen solo en la ficha de detalle (requeriría un scrape adicional por producto). Cruz Verde, EcoFarmacias y Salcobrand no lo exponen en ningún endpoint verificado. Ahumada quedó inconcluso (Demandware hidrata vía JS, no verificable sin navegador real). Recomendación: "Sprint B-lite" acotado a Dr. Simi + Farmex con match exacto por número de registro (nunca por nombre), badge distinto al heurístico actual ("Certificado por el ISP — Registro F-XXXXX/N" con link a la fuente), y cualquier extensión a fuzzy matching pasa primero por curación humana (reusando el campo `source` de `medications`, Sprint A) antes de mostrarse a usuarios. |
| 2026-07-31 | **Sprint C — alertas de precio por email en `web/`, implementado (sin RFC previo, diseño en `docs/prompt/claude/PROMPT_CLAUDE_SPRINT_C_ALERTAS_EMAIL.md`).** Sin cuenta de usuario: el email se captura al crear la alerta, gestión (confirmar/cancelar) vía token en la URL. Tabla nueva `email_alerts` en Supabase (flujo `pending → active → triggered` \| `unsubscribed`, disparo único por alerta, sin re-armado automático). Backend consolidado en **una sola función serverless nueva** (`api/api/alerts.ts`, delegando a `api/src/routes/alerts.ts`) que despacha por query param `action` (`create` implícito en POST, `confirm`, `unsubscribe`, `check`) — decisión explícita para no acercarse al límite de 12 funciones del plan Hobby de Vercel (queda en 9 de 12). `action=check` (cron) protegido por `CRON_SECRET` dedicado, sin fallback abierto a diferencia de `API_SECRET_KEY`. Cron diario nuevo en GitHub Actions (`check-price-alerts.yml`), mismo patrón que `update-branches.yml`. Reusa `searchMedications()` del `searchService` existente para revisar precios reales (agrupando por `canonicalName` para no re-buscar el mismo medicamento dos veces). Email vía Resend, dominio sandbox (`onboarding@resend.dev`) — decisión explícita del CEO para no bloquear el sprint por verificación de dominio propio. UI en `web/`: `PriceAlertForm` en `/medicamento/[slug]`, con Server Action `createPriceAlert.ts`. 28 tests nuevos en `api/` (`emailAlertsDb.test.ts` + `alerts.test.ts`) + 8 en `web/`; toda la suite de ambos workspaces verde; typecheck limpio. Cero cambios en `mobile/`, `packages/domain/` ni el contrato de `/api/search`. |
| 2026-08-02 | **Sprint D — cuenta ligera + perfil de usuario en `web/`, implementado (reabierto a pedido explícito del CEO, diseño en `docs/prompt/claude/PROMPT_CLAUDE_SPRINT_D_CUENTA_LIGERA.md`).** Alcance acotado, confirmado por el CEO antes de escribir código: sin flujo de pago todavía, login con el mecanismo que ya existe (Supabase Auth, email/contraseña). Nueva tabla `profiles` (`id` = `auth.users.id`, `email`, `plan: 'free'\|'premium'`) con trigger `on_auth_user_created` (auto-crea la fila al registrarse) y RLS: el usuario puede **leer** su propio perfil pero no escribirlo — solo `/admin/usuarios` (vía `SUPABASE_SECRET_KEY`, bypassea RLS) puede cambiar el plan, evitando que un usuario se auto-asigne premium. `auth/callback/route.ts` generalizado con `?next=` (default `/admin`, sin cambio de comportamiento para el login existente) para servir también la confirmación de email de `/cuenta/registro`. `proxy.ts` extendido con un branch `/cuenta/:path*` que gatea solo por sesión (sin lista blanca, a diferencia de `/admin`). Páginas nuevas: `/cuenta/registro`, `/cuenta/ingresar` (mismo patrón que `/admin/login` sin el botón de Google), `/cuenta` (email + plan + cerrar sesión). Vista admin nueva `/admin/usuarios` con toggle de plan, mismo patrón que `feedbackAdmin.ts`/`/admin/feedback`. Tests nuevos para `profilesAdmin.ts` (5, primera cobertura de test en el subsistema `/admin`); resto de la suite de `web/` (21 archivos) y de `api/` verde sin cambios; typecheck limpio en ambos workspaces. Cero cambios en `mobile/`, `packages/domain/` ni en el login de `/admin`. **Pendiente de Mario**: correr la sección "Sprint D" de `docs/database/schema.sql` en Supabase. |
| 2026-08-02 | **Corrección de documentación: la migración de cuentas de infraestructura no es hacia "LET" sino hacia `mario.lillo.alfaro@gmail.com`.** El CEO corrigió una premisa incorrecta en `docs/release/SERVICE_ACCOUNT_MIGRATION.md` y otros 3 documentos (`PLAY_CONSOLE_CHECKLIST.md`, `PRODUCTION_BLOCKERS_PLAN.md`, acta `20260731.md`): nunca existió una cuenta separada de "LET" como destino de la consolidación de servicios externos — el destino real es la cuenta personal `mario.lillo.alfaro@gmail.com`, que además ya es dueña de Google Play Console y de Khipu (verificado por las iniciales/nombre del titular), por lo que esos dos servicios no requerían ninguna migración, solo confirmación. Se eliminaron todas las referencias a "LET"/"letchile" de la documentación (`docs/`) y del email de contacto real (`docs/privacy-policy.html`, `web/.env.example`), reemplazándolas por `mario.lillo.alfaro@gmail.com`. El dominio de envío de Resend (`from:`) queda como pendiente aparte — es un tema técnico distinto (Resend exige verificar un dominio propio por DNS, y `gmail.com` no aplica para eso), no resuelto por esta corrección. |
| 2026-08-02 | **Registrada la Epic "Subscription Platform" y el papeleo de su Fase 1 (Motor de Suscripciones) — sin implementar todavía.** A pedido del CEO de construir un motor de suscripciones independiente de proveedor de pago (backend como única fuente de verdad, proveedores que solo notifican, planes configurables no hardcodeados), se registró: Epic completa en `docs/product/EPICS.md` (4 fases: motor+Google Play, Web Billing+Stripe, Apple, Plataforma Comercial); entrada en `BACKLOG_PRODUCT.md` con CFPS de Fase 1 = **3.0** (Media — mismo rango que Sprint A/CFM-ID: infraestructura habilitante de bajo valor de usuario directo pero alto impacto estratégico); `RFC-003_SUBSCRIPTION_ENGINE.md` (modelo de datos: `subscription_plans`/`subscriptions`/`subscription_events`; servicio `getEntitlement`/`recordProviderEvent`/`grantManual`; adaptador Google Play; API consolidada en `api/api/subscriptions.ts`, quedando en 10/12 funciones Vercel); `ADR-0002_SUBSCRIPTION_ARCHITECTURE.md`; issues `CF-112` a `CF-116`. Confirmado que `docs/product/SUBSCRIPTION_STRATEGY.md` ya existía con la estrategia de negocio aprobada — no se duplicó, solo se referenció. Reemplaza `profiles.plan` (Sprint D) como fuente de verdad, dejándolo como cache derivado (CF-116). **Riesgo central documentado explícitamente**: `mobile/` sigue en Prueba Cerrada de Google Play — el motor se puede construir y probar del lado `api/`, pero el flujo de compra real iniciado desde la app (y por lo tanto la verificación end-to-end) queda bloqueado hasta que `mobile/` se libere. Ningún código implementado todavía — queda pendiente que el CEO ratifique el CFPS antes de generar el prompt de sprint (Regla 2 del `PRODUCT_DECISION_FRAMEWORK.md`). |
| 2026-08-02 | **Sprint Subscription Platform Fase 1 — Motor de Suscripciones, implementado y mergeado a `main`.** CEO ratificó el CFPS 3.0 sin ajustes y autorizó comenzar. Entregado: modelo de datos aditivo en Supabase (`subscription_plans`, `subscriptions`, `subscription_events`, con RLS cerrado para `authenticated` — solo el cliente admin lee/escribe — y plan placeholder `cortesia` para satisfacer el FK de `grantManual`); `api/src/lib/subscriptionsDb.ts` (capa de datos, degradación elegante si Supabase no responde, mismo patrón que `priceHistoryDb.ts`); `api/src/services/subscriptionService.ts` (`getEntitlement`, `recordProviderEvent`, `grantManual`, `revokeManual`); `api/src/lib/adapters/googlePlayAdapter.ts` (parseo de notificaciones RTDN de Google Play, sin tocar `mobile/`); endpoint consolidado `api/api/subscriptions.ts` (`action=me\|verify-purchase\|google-rtdn\|grant-manual\|revoke-manual`, quedando en 10/12 funciones Vercel). `web/src/lib/profile.ts` y `profilesAdmin.ts` migrados (CF-116): dejan de leer/escribir `profiles.plan` directo y pasan por el motor vía HTTP a `api/`, degradando siempre a `free`/no-op si `api/` falla — sin cambio de comportamiento visible en `/cuenta` ni `/admin/usuarios`. 131 tests nuevos en `api/` + 10 nuevos en `web/` (7 en `profile.test.ts`, 3 en `profilesAdmin.test.ts`), toda la suite de ambos workspaces verde, typecheck limpio. Cero cambios en `mobile/` ni en `packages/domain/`. Riesgo R-02 (Google RTDN no trae `user_id`, solo `purchaseToken`) queda documentado y sin resolver hasta que `mobile/` salga de Prueba Cerrada y pueda enviar el purchase token al backend al momento de la compra. **Pendiente de Mario**: correr el SQL nuevo de `docs/database/schema.sql` en Supabase; crear la Service Account de Google Cloud bajo `mario.lillo.alfaro@gmail.com` y configurar `GOOGLE_RTDN_SECRET` en Vercel cuando se quiera probar el flujo real de Google Play (CF-114). |
| 2026-08-02 | **Sprint Subscription Platform Fase 2 — Web Billing con Stripe, implementado y mergeado a `main`.** CEO autorizó papeleo + implementación en el mismo pedido (CFPS 3.20, sin pausa de ratificación como en Fase 1). Entregado: columna aditiva `subscription_plans.stripe_price_id`; `api/src/lib/adapters/stripeAdapter.ts` (verificación de firma de webhooks con `node:crypto`, sin el SDK `stripe` — ver ADR-0003; parsing de `checkout.session.completed`/`customer.subscription.updated`/`customer.subscription.deleted`, `invoice.payment_failed` y otros tipos ignorados explícitamente); 3 acciones nuevas en el endpoint consolidado `api/api/subscriptions.ts` (`plans` público, `create-checkout-session` con sesión, `stripe-webhook` con firma — sigue en 10/12 funciones Vercel; `bodyParser` desactivado para preservar el body crudo de la firma); `web/`: `getAvailablePlans()`, botón "Actualizar a Premium" en `/cuenta` (solo visible con al menos un plan vendible configurado — hoy ninguno, catálogo vacío a propósito) y Server Action `createCheckoutSession` con redirect a Stripe Checkout hospedado. 89 tests nuevos en `api/` + 8 en `web/`, toda la suite de ambos workspaces verde, typecheck limpio. Cero cambios en `mobile/` ni `packages/domain`. **Pendiente de Mario**: correr la sección "Fase 2" de `docs/database/schema.sql` en Supabase; crear/confirmar cuenta de Stripe bajo `mario.lillo.alfaro@gmail.com`; configurar `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` y `WEB_APP_URL` en Vercel; definir y crear el primer plan comercial real (Stripe Price + fila en `subscription_plans`) — recién ahí aparece el botón de upgrade en producción. |
