{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm dlx vercel dev",
    "deploy:preview": "pnpm dlx vercel deploy",
    "deploy:prod": "pnpm dlx vercel deploy --prod",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
